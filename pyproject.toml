[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "randcracks"
version = "0.0.0"
description = "Crackers for MT19937, xorshift128+, and glibc random"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["python_mt19937"]
